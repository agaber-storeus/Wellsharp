@extends('layouts.admin')
@section('admin-content')<div class="page-head"><h1>{{ $course->name }}</h1><div class="actions"><a class="btn secondary" href="{{ route('admin.courses.questions.index', $course) }}">Questions</a><a class="btn secondary" href="{{ route('admin.courses.exams.index', $course) }}">Exams</a><a class="btn secondary" href="{{ route('admin.courses.edit', $course) }}">Edit Subject</a>@if($course->archived_at === null)<form method="POST" action="{{ route('admin.courses.archive', $course) }}">@csrf @method('PATCH')<button class="btn danger">Retire</button></form>@endif</div></div><div class="card"><p>{{ $course->description }}</p><p class="muted">Subject code: {{ $course->code }} · Provider: {{ $course->provider?->name ?: 'Not assigned' }}</p></div>@endsection
