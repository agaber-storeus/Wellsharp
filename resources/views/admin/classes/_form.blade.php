<div class="form-grid"><div class="field"><label for="class_number">Class number</label><input id="class_number" name="class_number" value="{{ old('class_number', $trainingClass->class_number) }}" required></div><div class="field"><label for="course_id">Subject</label><select id="course_id" name="course_id" required><option value="">Select Subject</option>@foreach($courses as $course)<option value="{{ $course->id }}" @selected(old('course_id', $trainingClass->course_id) == $course->id)>{{ $course->code }} - {{ $course->name }}</option>@endforeach</select></div><div class="field"><label for="training_provider_id">Training provider</label><select id="training_provider_id" name="training_provider_id"><option value="">Not assigned</option>@foreach($providers as $provider)<option value="{{ $provider->id }}" @selected(old('training_provider_id', $trainingClass->training_provider_id) == $provider->id)>{{ $provider->name }}</option>@endforeach</select></div><div class="field"><label for="starts_at">Starts</label><input id="starts_at" type="datetime-local" name="starts_at" value="{{ old('starts_at', $trainingClass->starts_at?->format('Y-m-d\TH:i')) }}"></div><div class="field"><label for="ends_at">Ends</label><input id="ends_at" type="datetime-local" name="ends_at" value="{{ old('ends_at', $trainingClass->ends_at?->format('Y-m-d\TH:i')) }}"></div><div class="field full"><label for="notes">Notes</label><textarea id="notes" name="notes">{{ old('notes', $trainingClass->notes) }}</textarea></div></div><div class="actions" style="margin-top:20px"><button class="btn">Save class</button><a class="btn secondary" href="{{ $trainingClass->exists ? route('admin.classes.show', $trainingClass) : route('admin.classes.index') }}">Cancel</a></div>
