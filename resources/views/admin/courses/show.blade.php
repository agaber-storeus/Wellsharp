@extends('layouts.admin')
@section('admin-content')<div class="admin-page-head hero"><div><h1>{{ $course->name }}</h1></div><div class="admin-page-actions"><a class="btn secondary" href="{{ route('admin.courses.questions.index', $course) }}">Questions</a><a class="btn secondary" href="{{ route('admin.courses.exams.index', $course) }}">Exams</a><a class="btn secondary" href="{{ route('admin.courses.edit', $course) }}">Edit Subject</a>@if($course->archived_at === null)<form method="POST" action="{{ route('admin.courses.archive', $course) }}">@csrf @method('PATCH')<button class="btn danger">Retire</button></form>@endif</div></div><div class="admin-bento"><div class="admin-bento-card admin-bento-card--wide"><div class="admin-card-head"><span class="admin-card-icon">📚</span><h3>Details</h3></div><p>{{ $course->description }}</p><p class="muted">Subject code: {{ $course->code }} · Provider: {{ $course->provider?->name ?: 'Not assigned' }}</p></div></div>@endsection
