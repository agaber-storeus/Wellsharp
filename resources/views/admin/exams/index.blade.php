@extends('layouts.admin')
@section('admin-content')<div class="page-head"><div><span class="admin-kicker">Subject · {{ $course->code }}</span><h1>Exams</h1><p>{{ $course->name }}</p></div><div class="actions"><a class="btn secondary" href="{{ route('admin.courses.show', $course) }}">Subject</a><a class="btn" href="{{ route('admin.courses.exams.create', $course) }}">Create exam</a></div></div><div class="card"><div class="table-wrap"><table class="table"><thead><tr><th>Name</th><th>Code</th><th>Questions</th><th>Schedules</th><th>Mode</th><th>Status</th><th></th></tr></thead><tbody>@forelse($exams as $exam)<tr><td><a href="{{ route('admin.exams.show', $exam) }}">{{ $exam->name }}</a></td><td>{{ $exam->code ?: '—' }}</td><td>{{ $exam->questions_count }}</td><td>{{ $exam->schedules_count }}</td><td>{{ $exam->question_order_mode->label() }}</td><td><span class="badge {{ $exam->status->value }}">{{ $exam->status->label() }}</span></td><td><a href="{{ route('admin.exams.show', $exam) }}">View</a></td></tr>@empty<tr><td colspan="7" class="muted">No exams found.</td></tr>@endforelse</tbody></table></div>{{ $exams->links('components.pagination') }}</div>@endsection
