@extends('layouts.admin')
@section('admin-content')<div class="page-head hero"><div><span class="admin-kicker">Exam Management</span><h1>Create exam</h1><p>Define the assessment and, when publishing, its first Group schedule &mdash; Proctor, Instructor, and Student interfaces see it immediately.</p></div></div><form method="POST" action="{{ $course ? route('admin.courses.exams.store', $course) : route('admin.exams.store') }}">@csrf @include('admin.exams._form')</form>@if(!$course)<script>document.getElementById('course_id')?.addEventListener('change',event=>{if(event.target.value){window.location='{{ route('admin.exams.create') }}?course_id='+encodeURIComponent(event.target.value)}});</script>@endif @endsection
