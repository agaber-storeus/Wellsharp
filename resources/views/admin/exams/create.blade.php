@extends('layouts.admin')
@section('admin-content')<div class="page-head"><div><span class="admin-kicker">Exam Management</span><h1>Create exam</h1><p>Define a reusable assessment. Scheduling happens in a separate workflow.</p></div></div><div class="card"><form method="POST" action="{{ $course ? route('admin.courses.exams.store', $course) : route('admin.exams.store') }}">@csrf @include('admin.exams._form')</form></div>@if(!$course)<script>document.getElementById('course_id')?.addEventListener('change',event=>{if(event.target.value){window.location='{{ route('admin.exams.create') }}?course_id='+encodeURIComponent(event.target.value)}});</script>@endif @endsection
